# Clerk authentication keys
# Get these from your Clerk dashboard → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_with_your_key
CLERK_SECRET_KEY=sk_test_replace_with_your_key

# Backend API URL
BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Feature flags (must be prefixed NEXT_PUBLIC_ to be available in browser)
NEXT_PUBLIC_FLAG_AI_GENERATION=true
NEXT_PUBLIC_FLAG_BILLING=true
NEXT_PUBLIC_FLAG_SALLA_INTEGRATION=false
NEXT_PUBLIC_FLAG_ZID_INTEGRATION=false
NEXT_PUBLIC_FLAG_PLUGIN_EMBED=true
NEXT_PUBLIC_FLAG_INBOX=true
NEXT_PUBLIC_FLAG_PIPELINE=true
NEXT_PUBLIC_FLAG_SHOPIFY_ORDER_HUB=true

# Phase 10 — TikTok Inbox + AI Content Assistant
NEXT_PUBLIC_FLAG_TIKTOK_INBOX=false
NEXT_PUBLIC_FLAG_CONTENT_ASSISTANT=true
NEXT_PUBLIC_FLAG_POST_SCHEDULER=true

# Portfolio / demo mode
# When true: bypasses Clerk auth so any visitor can click into /dashboard.
# Combine with NEXT_PUBLIC_USE_DUMMY_DATA=true to populate UI with sample data.
NEXT_PUBLIC_DEMO_MODE=true

# Demo data
NEXT_PUBLIC_USE_DUMMY_DATA=true

# Phase 10 — Observability
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
 
# Developer Admin Panel (Server-side only — no NEXT_PUBLIC_ prefix)
ADMIN_CLERK_USER_IDS=user_replace_with_your_clerk_id
